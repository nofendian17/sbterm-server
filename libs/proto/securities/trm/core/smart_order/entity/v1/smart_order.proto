syntax = "proto3";

// Reconstructed from stockbit.com webpack bundle (chunk 84882.8ffce90ef15dd07c.js,
// modules 23997, 61414) — protobuf-es message definitions. NOT an official schema.
// Enum smart order — dipakai oleh securities.trm.core.portfolio.entity.v2.Order.
package securities.trm.core.smart_order.entity.v1;

import "securities/trm/core/smart_order/trailing_stop/order/entity/v1/trailing_stop.proto";

option go_package = "github.com/nofendian17/sbterm-server/internal/infrastructure/stockbit/proto/securities/trm/core/smart_order/entity/v1;smartorderv1";

enum AssetType {
  ASSET_TYPE_UNSPECIFIED = 0;
  ASSET_TYPE_STOCK_REGULAR = 1;
  ASSET_TYPE_STOCK_RIGHT = 2;
  ASSET_TYPE_STOCK_WARRANT = 3;
  ASSET_TYPE_FR_BOND = 4;
  ASSET_TYPE_MUTUAL_FUND = 5;
}

enum MarketBoard {
  MARKET_BOARD_UNSPECIFIED = 0;
  MARKET_BOARD_REGULER = 1;
  MARKET_BOARD_TUNAI = 2;
}

enum OrderSide {
  ORDER_SIDE_UNSPECIFIED = 0;
  ORDER_SIDE_BUY = 1;
  ORDER_SIDE_SELL = 2;
}

enum OrderExpiryType {
  ORDER_EXPIRY_TYPE_UNSPECIFIED = 0;
  ORDER_EXPIRY_TYPE_GTC = 1;
  ORDER_EXPIRY_TYPE_GFD = 2;
  ORDER_EXPIRY_TYPE_FAK = 3;
}

enum OrderType {
  ORDER_TYPE_UNSPECIFIED = 0;
  ORDER_TYPE_LIMIT = 1;
  ORDER_TYPE_MARKET = 2;
}

enum OrderTriggerType {
  ORDER_TRIGGER_TYPE_UNSPECIFIED = 0;
  ORDER_TRIGGER_TYPE_LTE = 1;
  ORDER_TRIGGER_TYPE_GTE = 2;
}

enum OrderBookSide {
  ORDER_BOOK_SIDE_UNSPECIFIED = 0;
  ORDER_BOOK_SIDE_BID = 1;
  ORDER_BOOK_SIDE_ASK = 2;
}

// Dipakai sebagai enum status Order (module 23997, exports {i}).
enum OrderStatus {
  ORDER_STATUS_UNSPECIFIED = 0;
  ORDER_STATUS_READY = 1;
  ORDER_STATUS_AMENDED = 2;
  ORDER_STATUS_EXECUTED = 3;
  ORDER_STATUS_WITHDRAWN = 4;
  ORDER_STATUS_FORCE_DELETED = 5;
  ORDER_STATUS_CORP_ACT_DELETED = 6;
  ORDER_STATUS_EXPIRED = 7;
}
